#include<bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cin >> t;
	while(t--){
		string s;
		cin >> s;
		vector<int> a(0, s.size());
		vector<int> b(0, s.size());
		vector<int> c(0, s.size());
		for(int )
	}




}
